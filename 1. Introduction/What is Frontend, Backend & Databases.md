# 📘 Topic: Frontend, Backend & Databases

Understanding the three main parts of web development: **Frontend**, **Backend**, and **Databases**.

---

## 🧠 Overview

| Part         | Description                                                                         | Main Technologies                                |
| ------------ | ----------------------------------------------------------------------------------- | ------------------------------------------------ |
| **Frontend** | The part of the website or app users see and interact with (UI).                    | HTML, CSS, JavaScript, React, Angular, Vue.js    |
| **Backend**  | The behind-the-scenes part that handles logic, authentication, and data processing. | Node.js, Express, Django, Flask, Java, PHP, .NET |
| **Database** | Stores and retrieves data used by the backend.                                      | MongoDB, MySQL, PostgreSQL, Firebase, SQLite     |

---

## 🧩 Example — Full Stack Workflow

1️⃣ **Frontend (Client):**

* A user fills a form on the website.
* Example: React sends a request to the backend.

2️⃣ **Backend (Server):**

* Receives the request, processes it, and interacts with the database.
* Example: Express.js handles the form data and stores it in MongoDB.

3️⃣ **Database:**

* Saves the data permanently and sends a response back.
* Example: MongoDB stores user info and returns a success message.

---

## 🧩 Visual Flow

**Frontend (React)** → sends data to → **Backend (Express)** → stores in → **Database (MongoDB)** → response → **Frontend**

---

💡 **Tip:**
🖥️ **Frontend** = What users see.
⚙️ **Backend** = What powers the logic.
💾 **Database** = Where data lives.
Together, they form the **Full Stack** of a web application.
